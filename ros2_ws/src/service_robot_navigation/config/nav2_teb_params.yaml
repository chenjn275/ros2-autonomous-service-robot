# Optional Nav2 controller-server override for a TEB controller plugin.
# This file requires an external ROS2 package that exports:
#   teb_local_planner::TebLocalPlannerROS
# It is not used by default because the base demo is verified with DWB.
controller_server:
  ros__parameters:
    use_sim_time: true
    controller_frequency: 5.0
    odom_topic: /odom
    progress_checker_plugin: progress_checker
    goal_checker_plugins: [general_goal_checker]
    controller_plugins: [FollowPath]

    progress_checker:
      plugin: nav2_controller::SimpleProgressChecker
      required_movement_radius: 0.5
      movement_time_allowance: 10.0

    general_goal_checker:
      plugin: nav2_controller::SimpleGoalChecker
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.25
      stateful: true

    FollowPath:
      plugin: teb_local_planner::TebLocalPlannerROS
      teb_autosize: true
      dt_ref: 0.3
      dt_hysteresis: 0.1
      max_samples: 500
      max_global_plan_lookahead_dist: 3.0
      global_plan_viapoint_sep: 0.3
      global_plan_prune_distance: 1.0
      max_vel_x: 0.26
      max_vel_x_backwards: 0.1
      max_vel_theta: 1.0
      acc_lim_x: 2.5
      acc_lim_theta: 3.2
      min_turning_radius: 0.0
      wheelbase: 0.0
      cmd_angle_instead_rotvel: false
      footprint_model:
        type: circular
        radius: 0.24
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.25
      free_goal_vel: false
      min_obstacle_dist: 0.27
      inflation_dist: 0.6
      include_costmap_obstacles: true
      include_dynamic_obstacles: false
      no_inner_iterations: 5
      no_outer_iterations: 4
      optimization_activate: true
      optimization_verbose: false
      penalty_epsilon: 0.1
      weight_max_vel_x: 0.5
      weight_max_vel_theta: 0.5
      weight_acc_lim_x: 0.5
      weight_acc_lim_theta: 10.5
      weight_kinematics_nh: 1000.0
      weight_kinematics_forward_drive: 3.0
      weight_optimaltime: 1.0
      weight_obstacle: 100.0
      weight_viapoint: 50.0
      enable_homotopy_class_planning: true
      enable_multithreading: true
      max_number_classes: 4
      shrink_horizon_backup: true
      oscillation_recovery: true
