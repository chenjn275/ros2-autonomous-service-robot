# Assets

本目录用于放置轻量级说明图片、RViz 截图或小尺寸示意图。

不要提交以下内容：

- YOLO/深度学习权重文件，例如 `.pt`、`.onnx`、`.engine`
- 数据集、rosbag 大文件
- Gazebo/仿真生成缓存
- build/install/log 输出

模型权重和数据集应通过 README 中的外部路径参数配置，例如 `model_path:=/path/to/model.pt`。
